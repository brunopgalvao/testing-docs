name: Meta Setup (All-in-One)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:

jobs:
  setup-all:
    name: Setup on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Setup Scripts (Linux & macOS)
        run: |
          bash .github/scripts/setup-rust.sh 1.86
          bash .github/scripts/setup-chain-spec-builder.sh 10.0.0
          bash .github/scripts/setup-omni-node.sh 0.5.0
          bash .github/scripts/build-parachain.sh stable stable2412
